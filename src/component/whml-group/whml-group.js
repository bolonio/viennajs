import React from "react";

// { layout.map((component, i) => <div key={i}>{ component }</div>) }

export default function Group(props: Props) {
    console.debug("props", props)
    return (
        <div className="whml-group border margin">
            
        </div>
    );
}